1. main으로 이동
```bash
git checkout main
```

2. 최신 main 받기
```bash
git pull origin main
```

3. 작업용 브랜치 생성
```bash
git checkout -b 브랜치명
```
4. 브랜치 확인
```bash
git branch
```

5. 변경사항 확인
```bash
git status
```

6. 변경사항 확인
```bash
git status
```

7. 변경사항 git에 추가
```bash
git add .
또는
git add 파일 경로
```

8. 변경사항 git에 추가
```bash
git commit -m "메시지 입력"
```

9. GitHub에 브랜치 올리기
```bash
git push -u origin 브랜치명
```